<UserControl x:Class="NINA.Plugins.PolarAlignment.OAPA.OAPAControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:NINA.Plugins.PolarAlignment.OAPA"
             mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <s:String x:Key="OAPASpeedToolTip">Speed value passed to OAPA move commands for this axis.</s:String>
        <s:String x:Key="OAPAGearRatioToolTip">Scale factor used when converting between OAPA motor units and the axis units shown by TPPA.</s:String>
        <s:String x:Key="OAPARunCurrentToolTip">Run-current value, in milliamps, sent to the OAPA controller for this axis.</s:String>
        <s:String x:Key="OAPAHoldPercentToolTip">Hold-current percentage sent to the OAPA controller for this axis.</s:String>
        <s:String x:Key="OAPAYBacklashToolTip">Altitude-axis mechanical backlash in arcminutes, measured automatically by the Self-Calibration. Specific to OAPA platforms.</s:String>
        <s:String x:Key="OAPABacklashModeToolTip">How this axis handles its backlash on direction reversals. Off: no compensation (negligible backlash). Soft: single move extended by 75% of the backlash (conservative if the value may be overestimated). Full: single move extended by the whole backlash. Unidirectional: overshoot past the target and return, so the final approach always comes from the same direction (best for large backlash). Applying a calibration sets the recommended mode automatically.</s:String>
    </UserControl.Resources>

    <StackPanel>
        <!-- Motor Settings -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!-- Azimuth Settings -->
            <GroupBox Header="Azimuth Motor Settings">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPASpeedToolTip}">Speed</TextBlock>
                    <ComboBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="5" SelectedValue="{Binding XSpeed}" ToolTip="{StaticResource OAPASpeedToolTip}">
                        <ComboBox.ItemsSource>
                            <x:Array Type="{x:Type s:Int32}">
                                <s:Int32>100</s:Int32>
                                <s:Int32>200</s:Int32>
                                <s:Int32>300</s:Int32>
                                <s:Int32>400</s:Int32>
                                <s:Int32>500</s:Int32>
                                <s:Int32>600</s:Int32>
                                <s:Int32>700</s:Int32>
                                <s:Int32>800</s:Int32>
                                <s:Int32>900</s:Int32>
                                <s:Int32>1000</s:Int32>
                            </x:Array>
                        </ComboBox.ItemsSource>
                    </ComboBox>
                    <TextBlock Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPAGearRatioToolTip}">Calibration Factor</TextBlock>
                    <TextBox Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" Margin="5" Text="{Binding XGearRatio}" ToolTip="{StaticResource OAPAGearRatioToolTip}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPARunCurrentToolTip}">Run (mA)</TextBlock>
                    <TextBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="5" Text="{Binding XRunCurrent}" ToolTip="{StaticResource OAPARunCurrentToolTip}" />
                    <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPAHoldPercentToolTip}">Hold %</TextBlock>
                    <TextBox Grid.Row="1" Grid.Column="3" VerticalAlignment="Center" Margin="5" Text="{Binding XHoldPercent}" ToolTip="{StaticResource OAPAHoldPercentToolTip}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPABacklashModeToolTip}">Backlash mode</TextBlock>
                    <ComboBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Margin="5" ItemsSource="{Binding BacklashModeNames}" SelectedItem="{Binding XBacklashModeName}" ToolTip="{StaticResource OAPABacklashModeToolTip}" />
                </Grid>
            </GroupBox>

            <!-- Altitude Settings -->
            <GroupBox Grid.Column="1" Header="Altitude Motor Settings">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPASpeedToolTip}">Speed</TextBlock>
                    <ComboBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="5" SelectedValue="{Binding YSpeed}" ToolTip="{StaticResource OAPASpeedToolTip}">
                        <ComboBox.ItemsSource>
                            <x:Array Type="{x:Type s:Int32}">
                                <s:Int32>100</s:Int32>
                                <s:Int32>200</s:Int32>
                                <s:Int32>300</s:Int32>
                                <s:Int32>400</s:Int32>
                                <s:Int32>500</s:Int32>
                                <s:Int32>600</s:Int32>
                                <s:Int32>700</s:Int32>
                                <s:Int32>800</s:Int32>
                                <s:Int32>900</s:Int32>
                                <s:Int32>1000</s:Int32>
                            </x:Array>
                        </ComboBox.ItemsSource>
                    </ComboBox>
                    <TextBlock Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPAGearRatioToolTip}">Calibration Factor</TextBlock>
                    <TextBox Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" Margin="5" Text="{Binding YGearRatio}" ToolTip="{StaticResource OAPAGearRatioToolTip}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPARunCurrentToolTip}">Run (mA)</TextBlock>
                    <TextBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="5" Text="{Binding YRunCurrent}" ToolTip="{StaticResource OAPARunCurrentToolTip}" />
                    <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPAHoldPercentToolTip}">Hold %</TextBlock>
                    <TextBox Grid.Row="1" Grid.Column="3" VerticalAlignment="Center" Margin="5" Text="{Binding YHoldPercent}" ToolTip="{StaticResource OAPAHoldPercentToolTip}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPAYBacklashToolTip}">Backlash (')</TextBlock>
                    <TextBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Margin="5" Text="{Binding YBacklashCompensation}" ToolTip="{StaticResource OAPAYBacklashToolTip}" />
                    <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Margin="5" ToolTip="{StaticResource OAPABacklashModeToolTip}">Backlash mode</TextBlock>
                    <ComboBox Grid.Row="2" Grid.Column="3" VerticalAlignment="Center" Margin="5" ItemsSource="{Binding BacklashModeNames}" SelectedItem="{Binding YBacklashModeName}" ToolTip="{StaticResource OAPABacklashModeToolTip}" />
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Manual Control Panel - Only Visible When Connected And Manual -->
        <Grid Margin="0,5,0,0">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Connected}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DoAutomatedAdjustments}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <s:Single x:Key="NegativeDotOne">-0.1</s:Single>
                <s:Single x:Key="DotOne">0.1</s:Single>
                <s:Single x:Key="NegativeOne">-1</s:Single>
                <s:Single x:Key="One">1</s:Single>
                <s:Single x:Key="NegativeTen">-10</s:Single>
                <s:Single x:Key="Ten">10</s:Single>
            </Grid.Resources>

            <!-- Azimuth Control -->
            <GroupBox Header="Azimuth">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Text="{Binding PositionX, StringFormat=N2}" />

                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Button Height="25" Margin="0,0,2.5,0" Command="{Binding NudgeXCommand}" CommandParameter="{StaticResource NegativeDotOne}">-0.1</Button>
                        <Button Grid.Column="1" Height="25" Margin="2.5,0,0,0" Command="{Binding NudgeXCommand}" CommandParameter="{StaticResource DotOne}">+0.1</Button>

                        <Button Grid.Row="1" Height="25" Margin="0,5,2.5,0" Command="{Binding NudgeXCommand}" CommandParameter="{StaticResource NegativeOne}">-1</Button>
                        <Button Grid.Row="1" Grid.Column="1" Height="25" Margin="2.5,5,0,0" Command="{Binding NudgeXCommand}" CommandParameter="{StaticResource One}">+1</Button>

                        <Button Grid.Row="2" Height="25" Margin="0,5,2.5,0" Command="{Binding NudgeXCommand}" CommandParameter="{StaticResource NegativeTen}">-10</Button>
                        <Button Grid.Row="2" Grid.Column="1" Height="25" Margin="2.5,5,0,0" Command="{Binding NudgeXCommand}" CommandParameter="{StaticResource Ten}">+10</Button>

                        <TextBox Grid.Row="3" Margin="0,5,2.5,0" VerticalAlignment="Center" HorizontalContentAlignment="Right" Text="{Binding TargetPositionX}" />
                        <Button Grid.Row="3" Grid.Column="1" Height="25" Margin="2.5,5,0,0" Command="{Binding MoveXCommand}">Move</Button>
                    </Grid>
                </Grid>
            </GroupBox>

            <!-- Altitude Control -->
            <GroupBox Grid.Column="1" Header="Altitude">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Text="{Binding PositionY, StringFormat=N2}" />

                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Button Height="25" Margin="0,0,2.5,0" Command="{Binding NudgeYCommand}" CommandParameter="{StaticResource NegativeDotOne}">-0.1</Button>
                        <Button Grid.Column="1" Height="25" Margin="2.5,0,0,0" Command="{Binding NudgeYCommand}" CommandParameter="{StaticResource DotOne}">+0.1</Button>

                        <Button Grid.Row="1" Height="25" Margin="0,5,2.5,0" Command="{Binding NudgeYCommand}" CommandParameter="{StaticResource NegativeOne}">-1</Button>
                        <Button Grid.Row="1" Grid.Column="1" Height="25" Margin="2.5,5,0,0" Command="{Binding NudgeYCommand}" CommandParameter="{StaticResource One}">+1</Button>

                        <Button Grid.Row="2" Height="25" Margin="0,5,2.5,0" Command="{Binding NudgeYCommand}" CommandParameter="{StaticResource NegativeTen}">-10</Button>
                        <Button Grid.Row="2" Grid.Column="1" Height="25" Margin="2.5,5,0,0" Command="{Binding NudgeYCommand}" CommandParameter="{StaticResource Ten}">+10</Button>

                        <TextBox Grid.Row="3" Margin="0,5,2.5,0" VerticalAlignment="Center" HorizontalContentAlignment="Right" Text="{Binding TargetPositionY}" />
                        <Button Grid.Row="3" Grid.Column="1" Height="25" Margin="2.5,5,0,0" Command="{Binding MoveYCommand}">Move</Button>
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Home Position - Only Visible When Connected -->
        <GroupBox Header="Home Position"
                  Margin="0,5,0,0"
                  Visibility="{Binding Connected, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Height="25" Margin="5" Command="{Binding GoHomeCommand}"
                        ToolTip="Move both axes back to the home position stored in this session">Go Home</Button>
                <Button Grid.Column="1" Height="25" Margin="5" Command="{Binding SetHomeCommand}"
                        ToolTip="Store the current axis positions as the home position for this session. The controller's position counter restarts at 0 on power-up, so the stored home is discarded on reconnect.">Set Home</Button>
                <StackPanel Grid.Column="2" Margin="5" Orientation="Horizontal" VerticalAlignment="Center"
                            Visibility="{Binding HasHome, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="X: " />
                    <TextBlock Text="{Binding HomeX, StringFormat=N2}" />
                    <TextBlock Text="  Y: " />
                    <TextBlock Text="{Binding HomeY, StringFormat=N2}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Self-Calibration Panel - Only Visible When Connected -->
        <GroupBox Header="Self-Calibration"
                  Margin="0,5,0,0"
                  Visibility="{Binding Connected, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Height="25" Margin="5" Padding="10,0" Command="{Binding CalibrateGearRatiosCommand}">Calibrate</Button>
                    <TextBlock Grid.Column="1" Margin="5" VerticalAlignment="Center" TextWrapping="Wrap" Text="{Binding CalibrationStatus}" />
                </Grid>

                <Grid Margin="5,5,5,0" Visibility="{Binding HasCalibrationResult, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="4" Margin="0,0,0,10"
                               FontWeight="Bold" FontSize="14"
                               Text="&#x2705; Calibration complete &#x2014; review the discovered values below and click Apply or Discard." />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,10,5" Text="Discovered X factor:" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,20,5" Text="{Binding DiscoveredXRatio, StringFormat=F2}" FontWeight="Bold" />
                    <TextBlock Grid.Row="1" Grid.Column="2" Margin="0,0,10,5" Text="Current X factor:" />
                    <TextBlock Grid.Row="1" Grid.Column="3" Margin="0,0,0,5" Text="{Binding XGearRatio, StringFormat=F2}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,10,5" Text="Discovered Y factor:" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,0,20,5" Text="{Binding DiscoveredYRatio, StringFormat=F2}" FontWeight="Bold" />
                    <TextBlock Grid.Row="2" Grid.Column="2" Margin="0,0,10,5" Text="Current Y factor:" />
                    <TextBlock Grid.Row="2" Grid.Column="3" Margin="0,0,0,5" Text="{Binding YGearRatio, StringFormat=F2}" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,10,5" Text="Discovered X backlash (arcmin):" />
                    <TextBlock Grid.Row="3" Grid.Column="1" Margin="0,0,20,5" Text="{Binding DiscoveredXBacklash, StringFormat=F2}" FontWeight="Bold" />
                    <TextBlock Grid.Row="3" Grid.Column="2" Margin="0,0,10,5" Text="Discovered Y backlash (arcmin):" />
                    <TextBlock Grid.Row="3" Grid.Column="3" Margin="0,0,0,5" Text="{Binding DiscoveredYBacklash, StringFormat=F2}" FontWeight="Bold" />

                    <TextBlock Grid.Row="4" Grid.ColumnSpan="4" Margin="0,5,0,5" TextWrapping="Wrap" Text="{Binding CalibrationConsistencyMessage}" FontStyle="Italic" />

                    <Grid Grid.Row="5" Grid.ColumnSpan="4" Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Height="25" Margin="0,0,2.5,0" Command="{Binding ApplyCalibrationCommand}">Apply</Button>
                        <Button Grid.Column="1" Height="25" Margin="2.5,0,0,0" Command="{Binding DiscardCalibrationCommand}">Discard</Button>
                    </Grid>
                </Grid>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>
